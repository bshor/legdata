# legdata

Utility functions for working with Shor legislative data.

## Installation

```r
install.packages("pak")
pak::pak("bshor/legdata")
```

The optional `descriptives.st.yr.outcomes()` plot helper uses `ggslopegraph`.
Install it separately when needed:

```r
pak::pak("bshor/ggslopegraph")
```
